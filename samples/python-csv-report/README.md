# CSV Report

A command line tool: read a folder of CSV sales files, clean them up,
work out monthly totals, and write an HTML report.

    python -m reportkit.cli data/ --out report.html --min-rows 5
